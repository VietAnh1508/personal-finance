function App() {
  return (
    <main className="min-h-screen bg-brand-bg px-6 py-20 text-brand-ink">
      <section className="mx-auto max-w-2xl rounded-2xl border border-amber-800/20 bg-white/70 p-8 shadow-sm">
        <p className="text-sm font-semibold uppercase tracking-[0.2em] text-brand-accent">Web Migration</p>
        <h1 className="mt-3 text-3xl font-bold">Personal Finance React SPA Baseline</h1>
        <p className="mt-4 text-base leading-7">
          Vite + TypeScript + Tailwind are configured. This is the starter shell for migration tasks.
        </p>
      </section>
    </main>
  );
}

export default App;
